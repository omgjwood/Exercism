/*var HelloWorld = function() {};

HelloWorld.prototype.hello = function(input) {

    var name = input || 'World';
    return  'Hello, ' + name + '!';
};

module.exports = HelloWorld; */





var word = function () {};

Words.prototype.count = function (phrase) {
    var counts = {};
    phrase.split().(function (word) 
        { counts[word] = counts[word] || 0) + 1; };
    
    return counts;
};

module.exports = Word;
